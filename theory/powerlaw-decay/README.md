# theory/powerlaw-decay/ — MOVED (tombstone, 2026-07-13)

**The power-law-decay theory has left this repository.** Owner ruling 2026-07-13:
it is reflection on Buffer Stock Theory, not HAFiscal material.

## Where it lives now

- **Canonical source (full history preserved):** `llorracc/BufferStockTheory-Latest`,
  branch `20260713-powerlaw-decay-theory-port` (@ `aa935570`), path `theory/powerlaw-decay/`.
- **Public pages:** <https://llorracc.github.io/BufferStockTheory-Latest/powerlaw-decay-theory/>
  (this repo's `gh-pages` now serves redirect stubs at the old `/powerlaw-decay-theory/` paths).
- **Pre-port state, in THIS repo:** annotated tag **`powerlaw-theory-final-pre-port`**
  (@ `8ff59ba5`) keeps every `THEOREM-REF`-pinned sha reachable — recover the old corpus
  with `git show powerlaw-theory-final-pre-port:theory/powerlaw-decay/<file>`.

## Why this stub exists

The copy that used to sit here was a **stale 2026-07-06 snapshot** (merged into this base
branch from the retired theorem branch at `a9f11608`); it was removed to prevent divergence
from the canonical home. The theorem branch and its worktree (`.worktrees/powerlaw-theorem`)
were **deleted** (local and origin) — do **not** recreate them, and do **not** restore this
directory. Edit the theory in its new home.

`THEOREM-REF` comments elsewhere in this repo (e.g. `Code/HA-Models/decay_form/`) and in HARK
(`pf_decay.py`) now point at `BufferStockTheory-Latest @ <sha> :: theory/powerlaw-decay/`.
