# PROVENANCE — qe2442-proof-sync generation

**Generation label:** `qe2442-proof-sync` (same branch name in all three repos)
**Date generated:** 2026-06-11 (autonomous overnight run)
**Purpose:** sync the HAFiscal source to the content of the QE typeset proof
(VTeX SkyLaTeX, MS `QE2442`, proof source `qe2442.tex`, corrected proof of
2026-06; submitted production bundle `HAFiscal.zip` of 2026-04-29, Zenodo DOI
10.5281/zenodo.19120432), and make the Latest→Public→QE pipeline regenerate it
without hand-finishing.

## The triple

| Repo | Branch | Commit | Notes |
|---|---|---|---|
| HAFiscal-Latest | `qe2442-proof-sync` | 4da15865 | source of truth; cut from `master` (f58f93ab) |
| HAFiscal-Public | `qe2442-proof-sync` (= `main`) | 03cbc17 | generated by `make-repo-Latest-to-Public.sh` + content syncs |
| HAFiscal-QE | `qe2442-proof-sync` (= `main`) | 2d61e39 | generated by `make-repo-Public-to-QE.sh` (flattened single file) |
| HAFiscal-QE | `qe2442-proof-sync-wpa` (= `with-precomputed-artifacts`) | 71a0019 | precomputed-artifacts variant |

Built with HAFiscal-make @ 1b98e19 (`SKIP_PUSH=1 SKIP_DOCKER=1 /bin/bash
./make-repo-Public-to-QE.sh`, run 9, "workflow complete").

## What this generation contains (vs the 2026-04-29 submission)

1. The two resubmission footnotes (Campbell–Mankiw splurge equivalence;
   CRRA-welfare definition) — master commit 889cb213.
2. `campbell1989consumption` reference — master commit a4e4f215.
3. "Supplemental Appendix" terminology + the copyeditor's cosmetic edits,
   back-ported per-occurrence from the proof (85f93c8e, 939f06b2, 917c9aae,
   4da15865): percent→`\%`, quote style, cross-ref capitalization,
   heading/caption sentence case, number style, hyphenation, comma/dash
   tweaks, eq:income case punctuation, High-School naming.
4. Reference update per the proof: `crawley2024Parsimonious` →
   `crawley2025Parsimonious` (Crawley–Holm–Tretvoll 2025, *International
   Economic Review*, doi:10.1111/iere.70041).
5. `HAFiscal-Add-Refs.bib` populated (46 entries): every cited reference not
   in the shared system bib, so the build's bibtool regeneration of
   `HAFiscal.bib` is complete (verified: cited 84 = bib 84).
6. QE supplement pointers are now PIPELINE-NATIVE (HAFiscal-make
   `build-qe-submission.sh`): the in-text `Appendix~\ref{app:Model-without-splurge}`
   references become citations of `carroll2026welfareSupplement`, the
   template's web-link stub collapses to the one-line journal stub, and the
   supplement bib entry is guaranteed in the QE working+repo bibs.
   (These were hand-finishing in the 2026-04-29 bundle.)
7. **2026-06-12 completion pass (commits 9b932a62, c0f114f6):** the
   proof-diff incorporation audit
   (conclusions_private/20260612_proof-diff-incorporation-audit.md, on the
   TM-vs-MC branch) found the 2026-06-11 back-port had missed items; all are
   now incorporated:
   - 9 citation updates to the proof's published versions (allcott2022high,
     auclert2024IKC, Bardoczy2021, beraja2024size, bhandari2026efficiency,
     davila2025welfare, Du2025, ganong2024spending, graham2026mental) in
     HAFiscal.bib, HAFiscal-Add-Refs.bib, and all in-text cites.
   - 33 remaining copyedits (percent→`\%` in calibrationRecession Panel C,
     spelled-out numbers→numerals, nonlinear/nontargeted/redistributive,
     en-dashes, splurge double quotes, i.e.→"that is", first-order,
     steady-state, High-School, missing comma/period in Appendix-HANK).
   - Proof frontmatter statements (@local/metadata.ltx funding env +
     titlepage + acknowledgments.texinput): Econ-Ark thanks, Zenodo
     replication-package statement, GitHub updated-code pointer, and
     guarded responsibility/replication `\setpkgattr` statements.
   Re-audit vs `qe2442.tex` after regeneration: zero remaining authorial
   diffs (only VTeX production mechanics + the documented exclusions below).
   NOTE: the GitHub remotes of the qe2442-proof-sync branches are now STALE
   (they predate this pass); regenerated Public/QE not yet pushed.

## Deliberately NOT synced (documented differences vs the proof)

- **Welfare numbers**: VERIFIED MATCHING — this generation carries 2.13/1.82
  (table body via Code/.../Tables/CRRA2/welfare6.tex and the table note), the
  same as the proof. (An earlier draft of this file wrongly claimed a 2.15/1.83
  residual; that figure came from a stale pre-f58f93ab local master.) The
  TM-vs-MC branch's recomputation (2.20/1.81) is a separate research question
  on that branch, not a proof divergence.
- **Figure "consolidation": VERIFIED EQUIVALENT, no source difference** (2026-06-11
  re-examination; an earlier draft of this bullet wrongly claimed our source kept
  "two separate figures"). `Figures/splurge_estimation.tex` has always been the
  two-panel figure: panel (a) carries the sub-label `fig:aggmpclotterywin`,
  panel (b) `fig:liquwealthdistribution`, and our below-figure Note is word-for-
  word the proof's caption note. Rendered references already match the proof
  exactly ("Figure 1a", "Figure 1b", plain "Figure 1"). The proof's only changes
  were production-mechanical: panels flattened into one EPS (subcaption lines
  dropped, letters drawn in the image), the Note folded into \caption{}, and the
  sub-label ref replaced by a hardcoded `}a` suffix. See
  plans/20260611_figure-consolidation_plan.md (on the TM-vs-MC branch).
- VTeX production scaffolding (styledata/vmkcol, JATS bibliography, `\-`
  hyphenation hints, control-space tweaks, table-layout internals).
- Proof erratum NOT copied: the proof's footnote ``\footnote{`Predicted''…}``
  has an unbalanced quote; we use balanced ```` ``Predicted'' ````.

## Archival anchors (pre-generation states)

| What | Ref | Commit |
|---|---|---|
| Public as published 2026-01-11 | branch `archive/public_2026-01-11_published` + tag `archive-public_2026-01-11_published` | 2703583 |
| QE main as submitted 2026-04-28 | tag `archive-qe_2026-04-28_submitted` (= `origin/main` until next push) | 1db8322 |
| QE wpa as submitted 2026-04-28 | tag `archive-qe_2026-04-28_submitted_wpa` (= `origin/with-precomputed-artifacts`) | 81a33bd |

⚠️ The local HAFiscal-QE repo is an **ephemeral build artifact**: the Public→QE
pipeline wipes `.git/` (rsync `--delete-excluded`) and re-creates orphan
branches each run. Durable QE archival therefore lives on the GitHub remote —
push the archive tags **before** pushing rewritten branches.
